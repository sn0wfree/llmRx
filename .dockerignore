# VCS / IDE / OS junk
.git
.github
.gitignore
.idea
.vscode
*.swp
*.swo
.DS_Store
Thumbs.db

# Local build artifacts
*.exe
*.dll
*.so
*.dylib
*.test
*.out
*.log
llmRx

# Local DB + data + secrets (mount as volumes / inject as
# docker secrets instead of baking them into the image).
data/
*.db
*.db-*
secrets/

# Node + web build artifacts: there is no web/ source tree or
# npm step in the build pipeline. The admin UI ships as Go
# html/template, embedded via go:embed templates.

# The Dockerfile copies: build/llmRx (host-compiled binary),
# go.mod, go.sum. Everything else above is noise.