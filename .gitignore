# Binaries
*.exe
*.exe~
*.dll
*.so
*.dylib
llmRx

# Go
*.test
*.out
vendor/
go.work

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Web UI
web/node_modules/
web/.env
# Note: web/dist/ and internal/webui/dist/ are BOTH committed so the
# binary embeds the latest UI without requiring `npm run build` on every clone.

# Config (local overrides)
config.local.yml
*.env
