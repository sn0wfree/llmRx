apiVersion: v1
kind: Service
metadata:
  name: {{ include "llmrx.fullname" . }}
  labels:
    {{- include "llmrx.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "llmrx.selectorLabels" . | nindent 4 }}
